# check = slice(indexOfInfo[index-1], indexOfInfo[index])
#         newMeta = metadata[check]

        # if meta != "D":
        #     newMeta = newMeta+meta
        #     print(newMeta)
        # else:
        #     print(meta + " "+ newMeta)
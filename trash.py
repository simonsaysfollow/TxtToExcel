# check = slice(indexOfInfo[index-1], indexOfInfo[index])
#         newMeta = metadata[check]

        # if meta != "D":
        #     newMeta = newMeta+meta
        #     print(newMeta)
        # else:
        #     print(meta + " "+ newMeta)

        # for double in res:
        #     if double == "D":
        #     res.remove(double)


    #     temp = copy.copy(res)
    
    # for word in temp: 
    #     if word == "D":
    #         previousWord = res[res.index(word)-1] 
    #         print(previousWord)
    #         previousWord = previousWord + word
    #         print(previousWord)
    #         res[res.index(word)-1] = previousWord
    
    # print(temp)

    # dlstringarray = 
    # print(dlstringarray)

     # print(dlstringarray)
    # print("\n\n")
    # print(dlstringarray)
    # dlstringarray = [line.strip() for line in dlstringarray]
    # print("\n")
    # print(dlstringarray)
    # remove 'ANSI' from first element (It's a fixed header)

    # dlstringarray[0] = dlstringarray[0][5:]
    # print(dlstringarray[0][5:])
    # metadata = dlstringarray[0]
  
    # dlstringarray.remove(metadata)
            